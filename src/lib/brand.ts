/**
 * Product display name. Single source of truth for every place the brand
 * appears in the UI. The India edition (simplerosterai.com) ships as
 * SimpleRosterAI; the upstream US repo uses SimpleScheduleAI.
 */
export const APP_NAME = "SimpleRosterAI";
